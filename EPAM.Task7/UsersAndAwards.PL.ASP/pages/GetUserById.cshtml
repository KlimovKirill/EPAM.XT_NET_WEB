@{
    Layout = null;
}
@using UsersAndAwards.BLL.Dependencies

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Get User By ID</title>
</head>
<body>
    <form action="~/pages/GetUserById.cshtml" method="post">
        <label>Enter User's ID which you want to see:</label>
        <input type="number" name="id" value="" />

        <button type="submit">Submit</button>
        <button type="button" id="homeBtn">Home</button>
    </form>

    @{
        if (IsPost)
        {
            int uID = int.Parse(Request.Params["id"]);

            var _userBLL = UsersAndAwardsBLLDependencies.UserLogic;
            var _awardBLL = UsersAndAwardsBLLDependencies.AwardLogic;

            <div>
                ID | Name | Age <br />
                <img src=@_userBLL.GetById(uID).ImageURL height="25" width="25" />
                @_userBLL.GetById(uID)
            </div>
        }

    }
    <script src="~/js/HomeButton.js"></script>
</body>
</html>
